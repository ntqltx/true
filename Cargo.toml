[package]
name = "compiler"
version = "0.1.0"
edition = "2024"
build = "src/build.rs"

[lib]
name = "c"
crate-type = ["staticlib"]

[dependencies]
strum = { version = "0.28.0", features = ["derive"] }

[profile.release]
opt-level = 3